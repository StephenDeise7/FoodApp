<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ie.wit.foodapp">

    <application
        android:name=".main.FoodApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.Add"></activity>
        <activity android:name=".activities.Base"></activity>
        <activity android:name=".activities.Edit"></activity>
        <activity android:name=".activities.Favourites"></activity>
        <activity android:name=".activities.Foods"></activity>
        <activity android:name=".activities.Help"></activity>
        <activity android:name=".activities.Search"></activity>
        <activity android:name=".Menus.MainActivity"></activity>
        <activity android:name=".Menus.HomeMenu" />
        <activity android:name=".Menus.LoginActivity" />
        <activity android:name=".Menus.MainLoginActivity" />
        <activity android:name=".Menus.ProfileActivity" />
    </application>

</manifest>